# Create requirements.txt
requirements = '''Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Werkzeug==3.0.1
'''

with open(f"{project_dir}/requirements.txt", "w") as f:
    f.write(requirements)

# Create a README with instructions
readme = '''# Cyber Tech Member Registry System

A beautiful dark-themed Flask application for managing member data with public view and admin controls.

## Features

✨ **Public View** — Beautiful cyberpunk-themed read-only table
🔐 **Admin Authentication** — Secure login for CEO access  
📝 **Full CRUD** — Create, Read, Update, Delete members
🎨 **Modern UI** — Glassmorphism, neon accents, smooth animations
📱 **Responsive** — Works on all devices

## Installation

1. Navigate to the project folder:
```bash
cd cyber_members
```

2. Create virtual environment:
```bash
python -m venv venv
```

3. Activate virtual environment:
- Windows: `venv\\Scripts\\activate`
- Mac/Linux: `source venv/bin/activate`

4. Install dependencies:
```bash
pip install -r requirements.txt
```

5. Run the application:
```bash
python app.py
```

6. Open browser:
- Public View: http://localhost:5000/
- Admin Login: http://localhost:5000/login

## Default Admin Credentials

- **Email:** ceo@gmail.com
- **Password:** let.begin

## Database

SQLite database (`members.db`) is auto-created on first run with sample data.

## API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | /api/members | Add new member |
| PUT | /api/members/<id> | Update member |
| DELETE | /api/members/<id> | Delete member |

## Customization

Edit CSS variables in the HTML templates to change colors:
```css
:root {
  --neon: #00ff88;      /* Change accent color */
  --bg: #020408;        /* Change background */
}
```
'''

with open(f"{project_dir}/README.md", "w") as f:
    f.write(readme)

print("✓ Created requirements.txt")
print("✓ Created README.md")
print("\n" + "="*50)
print("🚀 PROJECT SETUP COMPLETE!")
print("="*50)