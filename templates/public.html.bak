# Create the beautiful Public View template (read-only)
public_html = '''<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Cyber Tech — Public Member Registry</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #020408;
      --panel: #0a0f1a;
      --card: #111827;
      --muted: #6b7280;
      --neon: #00ff88;
      --neon-dim: rgba(0, 255, 136, 0.1);
      --accent: #00d4ff;
      --danger: #ff3366;
      --warning: #ffaa00;
      --success: #00ff88;
      --glass: rgba(255, 255, 255, 0.03);
      --border: rgba(255, 255, 255, 0.06);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: var(--bg);
      color: #e5e7eb;
      font-family: 'Inter', sans-serif;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Animated background grid */
    .bg-grid {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: 
        linear-gradient(rgba(0, 255, 136, 0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0, 255, 136, 0.03) 1px, transparent 1px);
      background-size: 50px 50px;
      pointer-events: none;
      z-index: 0;
    }

    .bg-glow {
      position: fixed;
      width: 600px;
      height: 600px;
      background: radial-gradient(circle, rgba(0, 255, 136, 0.08) 0%, transparent 70%);
      top: -300px;
      right: -300px;
      pointer-events: none;
      z-index: 0;
      animation: pulse 4s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 0.5; transform: scale(1); }
      50% { opacity: 0.8; transform: scale(1.1); }
    }

    .container-fluid {
      position: relative;
      z-index: 1;
      padding: 40px 20px;
      max-width: 1400px;
      margin: 0 auto;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 40px;
      padding: 24px;
      background: var(--glass);
      border: 1px solid var(--border);
      border-radius: 16px;
      backdrop-filter: blur(10px);
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .brand-icon {
      width: 56px;
      height: 56px;
      background: linear-gradient(135deg, var(--neon-dim), transparent);
      border: 1px solid rgba(0, 255, 136, 0.2);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
      color: var(--neon);
    }

    .brand-text h1 {
      font-size: 24px;
      font-weight: 700;
      color: var(--neon);
      margin: 0;
      letter-spacing: 1px;
      text-transform: uppercase;
    }

    .brand-text p {
      font-size: 13px;
      color: var(--muted);
      margin: 4px 0 0 0;
    }

    .admin-link {
      padding: 10px 20px;
      background: transparent;
      border: 1px solid var(--border);
      border-radius: 8px;
      color: var(--muted);
      text-decoration: none;
      font-size: 14px;
      transition: all 0.3s ease;
    }

    .admin-link:hover {
      border-color: var(--neon);
      color: var(--neon);
      box-shadow: 0 0 20px rgba(0, 255, 136, 0.2);
    }

    /* Stats Bar */
    .stats-bar {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }

    .stat-card {
      background: var(--glass);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 20px;
      transition: all 0.3s ease;
    }

    .stat-card:hover {
      border-color: rgba(0, 255, 136, 0.3);
      transform: translateY(-2px);
    }

    .stat-label {
      font-size: 12px;
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 8px;
    }

    .stat-value {
      font-size: 28px;
      font-weight: 700;
      color: var(--neon);
      font-family: 'JetBrains Mono', monospace;
    }

    /* Search & Filter */
    .controls {
      display: flex;
      gap: 16px;
      margin-bottom: 24px;
      flex-wrap: wrap;
    }

    .search-box {
      flex: 1;
      min-width: 300px;
      position: relative;
    }

    .search-box input {
      width: 100%;
      padding: 14px 20px 14px 48px;
      background: var(--glass);
      border: 1px solid var(--border);
      border-radius: 10px;
      color: #fff;
      font-size: 14px;
      transition: all 0.3s ease;
    }

    .search-box input:focus {
      outline: none;
      border-color: var(--neon);
      box-shadow: 0 0 0 3px rgba(0, 255, 136, 0.1);
    }

    .search-box::before {
      content: "🔍";
      position: absolute;
      left: 16px;
      top: 50%;
      transform: translateY(-50%);
      opacity: 0.5;
    }

    .filter-select {
      padding: 14px 20px;
      background: var(--glass);
      border: 1px solid var(--border);
      border-radius: 10px;
      color: #fff;
      font-size: 14px;
      cursor: pointer;
      min-width: 160px;
    }

    .filter-select:focus {
      outline: none;
      border-color: var(--neon);
    }

    /* Table Container */
    .table-container {
      background: var(--glass);
      border: 1px solid var(--border);
      border-radius: 16px;
      overflow: hidden;
      backdrop-filter: blur(10px);
    }

    .table {
      width: 100%;
      border-collapse: separate;
      border-spacing: 0;
      margin: 0;
    }

    .table thead {
      background: rgba(0, 0, 0, 0.3);
    }

    .table th {
      padding: 20px;
      text-align: left;
      font-size: 12px;
      font-weight: 600;
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 1px;
      border-bottom: 1px solid var(--border);
    }

    .table td {
      padding: 20px;
      border-bottom: 1px solid var(--border);
      font-size: 14px;
      vertical-align: middle;
    }

    .table tbody tr {
      transition: all 0.3s ease;
      animation: fadeIn 0.5s ease forwards;
      opacity: 0;
    }

    .table tbody tr:hover {
      background: rgba(0, 255, 136, 0.03);
    }

    @keyframes fadeIn {
      to {
        opacity: 1;
      }
    }

    /* Member ID */
    .member-id {
      font-family: 'JetBrains Mono', monospace;
      color: var(--accent);
      font-weight: 600;
      font-size: 13px;
      background: rgba(0, 212, 255, 0.1);
      padding: 6px 12px;
      border-radius: 6px;
      display: inline-block;
    }

    /* Name */
    .member-name {
      font-weight: 600;
      color: #fff;
    }

    .member-contact {
      font-size: 12px;
      color: var(--muted);
      margin-top: 4px;
    }

    /* Status Badges */
    .badge {
      padding: 6px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .badge-active {
      background: rgba(0, 255, 136, 0.15);
      color: var(--success);
      border: 1px solid rgba(0, 255, 136, 0.3);
    }

    .badge-pending {
      background: rgba(255, 170, 0, 0.15);
      color: var(--warning);
      border: 1px solid rgba(255, 170, 0, 0.3);
    }

    .badge-inactive {
      background: rgba(255, 51, 102, 0.15);
      color: var(--danger);
      border: 1px solid rgba(255, 51, 102, 0.3);
    }

    /* Verified Badge */
    .verified-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 12px;
      background: rgba(0, 255, 136, 0.1);
      border: 1px solid rgba(0, 255, 136, 0.2);
      border-radius: 20px;
      color: var(--success);
      font-size: 12px;
      font-weight: 600;
    }

    .verified-badge.not-verified {
      background: rgba(107, 114, 128, 0.1);
      border-color: rgba(107, 114, 128, 0.2);
      color: var(--muted);
    }

    /* Progress Bar */
    .progress-container {
      width: 100%;
      max-width: 200px;
    }

    .progress-bar-bg {
      height: 8px;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 4px;
      overflow: hidden;
      position: relative;
    }

    .progress-bar-fill {
      height: 100%;
      background: linear-gradient(90deg, var(--neon), var(--accent));
      border-radius: 4px;
      transition: width 1s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }

    .progress-bar-fill::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
      animation: shimmer 2s infinite;
    }

    @keyframes shimmer {
      0% { transform: translateX(-100%); }
      100% { transform: translateX(100%); }
    }

    .progress-text {
      font-size: 12px;
      color: var(--muted);
      margin-top: 6px;
      font-family: 'JetBrains Mono', monospace;
    }

    /* Copy Button */
    .copy-btn {
      padding: 8px 16px;
      background: transparent;
      border: 1px solid var(--border);
      border-radius: 6px;
      color: var(--muted);
      font-size: 12px;
      cursor: pointer;
      transition: all 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 6px;
    }

    .copy-btn:hover {
      border-color: var(--neon);
      color: var(--neon);
      background: rgba(0, 255, 136, 0.05);
    }

    .copy-btn.copied {
      border-color: var(--success);
      color: var(--success);
    }

    /* Empty State */
    .empty-state {
      text-align: center;
      padding: 60px 20px;
      color: var(--muted);
    }

    .empty-state-icon {
      font-size: 48px;
      margin-bottom: 16px;
      opacity: 0.5;
    }

    /* Footer */
    .footer {
      margin-top: 40px;
      padding: 20px;
      text-align: center;
      color: var(--muted);
      font-size: 13px;
      border-top: 1px solid var(--border);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .header {
        flex-direction: column;
        gap: 20px;
        text-align: center;
      }

      .table-container {
        overflow-x: auto;
      }

      .table {
        min-width: 800px;
      }

      .controls {
        flex-direction: column;
      }

      .search-box {
        min-width: 100%;
      }
    }

    /* Scrollbar */
    ::-webkit-scrollbar {
      width: 8px;
      height: 8px;
    }

    ::-webkit-scrollbar-track {
      background: var(--bg);
    }

    ::-webkit-scrollbar-thumb {
      background: var(--border);
      border-radius: 4px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: var(--muted);
    }
  </style>
</head>
<body>
  <div class="bg-grid"></div>
  <div class="bg-glow"></div>

  <div class="container-fluid">
    <!-- Header -->
    <div class="header">
      <div class="brand">
        <div class="brand-icon">◈</div>
        <div class="brand-text">
          <h1>Cyber Tech Registry</h1>
          <p>Public Member Database • Verified & Secure</p>
        </div>
      </div>
      <a href="{{ url_for('login') }}" class="admin-link">🔐 Admin Access</a>
    </div>

    <!-- Stats -->
    <div class="stats-bar">
      <div class="stat-card">
        <div class="stat-label">Total Members</div>
        <div class="stat-value" id="totalCount">{{ members|length }}</div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Verified</div>
        <div class="stat-value" id="verifiedCount">
          {{ members|selectattr('verified', 'equalto', true)|list|length }}
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Active</div>
        <div class="stat-value" id="activeCount">
          {{ members|selectattr('status', 'equalto', 'active')|list|length }}
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Avg Progress</div>
        <div class="stat-value" id="avgProgress">
          {% if members %}{{ (members|sum(attribute='progress') / members|length)|int }}%{% else %}0%{% endif %}
        </div>
      </div>
    </div>

    <!-- Controls -->
    <div class="controls">
      <div class="search-box">
        <input type="text" id="searchInput" placeholder="Search by ID, name, email or phone...">
      </div>
      <select class="filter-select" id="statusFilter">
        <option value="all">All Status</option>
        <option value="active">Active</option>
        <option value="pending">Pending</option>
        <option value="inactive">Inactive</option>
      </select>
      <select class="filter-select" id="verifiedFilter">
        <option value="all">All Members</option>
        <option value="verified">Verified Only</option>
        <option value="unverified">Unverified</option>
      </select>
    </div>

    <!-- Table -->
    <div class="table-container">
      <table class="table" id="membersTable">
        <thead>
          <tr>
            <th>Member ID</th>
            <th>Full Name & Contact</th>
            <th>Status</th>
            <th>Verification</th>
            <th>Progress</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          {% for member in members %}
          <tr data-status="{{ member.status }}" data-verified="{{ 'verified' if member.verified else 'unverified' }}" data-search="{{ member.member_id.lower() }} {{ member.full_name.lower() }} {{ member.email.lower() }} {{ member.phone_number }}">
            <td>
              <span class="member-id">{{ member.member_id }}</span>
            </td>
            <td>
              <div class="member-name">{{ member.full_name }}</div>
              <div class="member-contact">📧 {{ member.email }}<br>📱 {{ member.phone_number }}</div>
            </td>
            <td>
              <span class="badge badge-{{ member.status }}">{{ member.status }}</span>
            </td>
            <td>
              {% if member.verified %}
              <span class="verified-badge">✓ Verified</span>
              {% else %}
              <span class="verified-badge not-verified">○ Pending</span>
              {% endif %}
            </td>
            <td>
              <div class="progress-container">
                <div class="progress-bar-bg">
                  <div class="progress-bar-fill" style="width: 0%" data-progress="{{ member.progress }}"></div>
                </div>
                <div class="progress-text">{{ member.progress }}% Complete</div>
              </div>
            </td>
            <td>
              <button class="copy-btn" onclick="copyID('{{ member.member_id }}', this)">
                📋 Copy ID
              </button>
            </td>
          </tr>
          {% else %}
          <tr>
            <td colspan="6">
              <div class="empty-state">
                <div class="empty-state-icon">📭</div>
                <h3>No members found</h3>
                <p>The database is currently empty.</p>
              </div>
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>

    <div class="footer">
      <p>🔒 Cyber Tech Member Registry System • Public View Only • Admin authentication required for modifications</p>
    </div>
  </div>

  <script>
    // Animate progress bars on load
    window.addEventListener('load', () => {
      document.querySelectorAll('.progress-bar-fill').forEach((bar, index) => {
        const progress = bar.getAttribute('data-progress');
        setTimeout(() => {
          bar.style.width = progress + '%';
        }, index * 100);
      });

      // Stagger row animations
      document.querySelectorAll('tbody tr').forEach((row, index) => {
        row.style.animationDelay = (index * 0.05) + 's';
      });
    });

    // Copy ID function
    function copyID(id, btn) {
      navigator.clipboard.writeText(id).then(() => {
        btn.innerHTML = '✓ Copied!';
        btn.classList.add('copied');
        setTimeout(() => {
          btn.innerHTML = '📋 Copy ID';
          btn.classList.remove('copied');
        }, 2000);
      });
    }

    // Search and Filter functionality
    const searchInput = document.getElementById('searchInput');
    const statusFilter = document.getElementById('statusFilter');
    const verifiedFilter = document.getElementById('verifiedFilter');

    function filterTable() {
      const searchTerm = searchInput.value.toLowerCase();
      const statusValue = statusFilter.value;
      const verifiedValue = verifiedFilter.value;

      document.querySelectorAll('tbody tr').forEach(row => {
        const searchData = row.getAttribute('data-search') || '';
        const status = row.getAttribute('data-status');
        const verified = row.getAttribute('data-verified');

        const matchesSearch = searchData.includes(searchTerm);
        const matchesStatus = statusValue === 'all' || status === statusValue;
        const matchesVerified = verifiedValue === 'all' || verified === verifiedValue;

        if (matchesSearch && matchesStatus && matchesVerified) {
          row.style.display = '';
        } else {
          row.style.display = 'none';
        }
      });
    }

    searchInput.addEventListener('input', filterTable);
    statusFilter.addEventListener('change', filterTable);
    verifiedFilter.addEventListener('change', filterTable);
  </script>
</body>
</html>'''

with open(f"{project_dir}/templates/public.html", "w") as f:
    f.write(public_html)

print("✓ Created templates/public.html")